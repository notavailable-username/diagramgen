# Google Gemini API Configuration
# Get your API key from: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# Optional: Model Configuration
# Specify which Gemini model to use (default: gemini-2.5-flash)
# GEMINI_MODEL=gemini-2.5-flash

# Optional: Retry Configuration
# Maximum number of retry attempts for API calls (default: 15)
# MAX_RETRIES=15

# Delay in seconds between retry attempts (default: 0.3)
# RETRY_DELAY=0.3

# Optional: Logging Configuration
# Logging level: DEBUG, INFO, WARNING, ERROR, CRITICAL (default: ERROR)
# LOG_LEVEL=ERROR
